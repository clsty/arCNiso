#!/bin/bash
# 此脚本可启用 archlinuxcn 源；并且，若中途失败，也可多次执行。
echo "下面开始添加 archlinuxcn 源。请注意，此过程耗时可能长达 6 分钟。"
echo '
[archlinuxcn]
Include = /etc/pacman.d/archlinuxcn-mirrorlist' > /etc/pacman.d/archlinuxcn.conf
sleep 3
pacman -Sy archlinuxcn-keyring --noconfirm
